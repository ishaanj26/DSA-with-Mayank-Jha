// Check if a SUBARRAY computes to zero
#include <iostream>
#include <vector>
using namespace std;

int main()
{
    vector<int> arr = {5, 3, 1, 2, 9};
    for (size_t i = 0; i < arr.size(); i++)
    {
        for (size_t j = i; j < arr.size(); j++)
        {

        }
    }

    return 0;
}