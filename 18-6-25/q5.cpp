// Soldier and bananas
#include <iostream>
using namespace std;

int main()
{
    int m, k, w;
    int sum;
    sum = k * (w * (w + 1) / 2);
    int borrow = sum - m;
    ;
    if (borrow > 0)
    {
        cout << "Borrowed money is->" << borrow;
    }
    return 0;
}